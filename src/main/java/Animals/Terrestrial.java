package Animals;

import Enums.DietType;

public class Terrestrial extends AnimalAttraction {

    public Terrestrial(String genera, DietType diet, int caloricContent, int requiredDailyCalories) {
        super(genera, diet, caloricContent, requiredDailyCalories);
    }
}
